version: "2"

linters:
  enable:
    - staticcheck
    - gosec
    - errcheck
    - revive
  settings:
    gosec:
      excludes:
        - G117 # exported struct fields named APIKey in provider config structs

formatters:
  enable:
    - gofumpt
    - goimports
