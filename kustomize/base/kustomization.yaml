# Base Kustomize configuration for glitchgate
# Usage: kubectl apply -k ./base

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - configmap.yaml
  - secrets.yaml
  - postgres.yaml

commonLabels:
  app: glitchgate
