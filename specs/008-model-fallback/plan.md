# Implementation Plan: Fallback Models

**Branch**: `008-model-fallback` | **Date**: 2026-03-11 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `/specs/008-model-fallback/spec.md`

## Summary

Add virtual model entries to `model_list` config. A virtual entry carries a `fallbacks` array of other model names instead of a direct `provider`/`upstream_model`. At startup, virtual chains are flattened and validated (cycle detection, unknown references). At runtime, both proxy handlers iterate the chain and retry on 5xx, 429, or network error, stopping at the first success or logging exhaustion. A new `fallback_attempts` column on `request_logs` records how many provider dispatches were made per request.

## Technical Context

**Language/Version**: Go 1.26.1 (module `codeberg.org/kglitchy/glitchgate`)
**Primary Dependencies**: chi/v5, go-resty/v3, cobra+viper, modernc.org/sqlite, goose/v3, testify/require
**Storage**: SQLite — one new migration (`014_add_fallback_attempts.sql`)
**Testing**: `go test -race ./...`, table-driven, testify/require
**Target Platform**: Linux/macOS server, CGO_ENABLED=0
**Project Type**: HTTP proxy / web service
**Performance Goals**: Fallback chain selection adds zero measurable overhead; hot-path cost is one map lookup plus a loop over a pre-computed slice
**Constraints**: No allocations on the model-resolution hot path beyond the pre-computed slice; no goroutine-per-token
**Scale/Scope**: Config-sized data (tens of model entries)

## Constitution Check

| Principle | Status | Notes |
|-----------|--------|-------|
| I. Speed | ✅ PASS | Chain flattened at startup; dispatch loop is O(N) over a pre-allocated slice; no allocations on hot path |
| II. Resources | ✅ PASS | No new dependencies; pre-computed chains are config-sized; single binary unchanged |
| III. Clean Abstractions | ✅ PASS | `Provider` interface untouched; fallback logic isolated to config resolution + handler dispatch; no cross-cutting state |
| IV. Correctness | ✅ PASS | Streaming fallback checked before `w.WriteHeader`; request body replayed unchanged; existing streaming relay untouched |
| V. Security | ✅ PASS | No new auth surface; cycle detection prevents infinite loops at startup; no secrets involved |

No violations. No Complexity Tracking table required.

## Project Structure

### Documentation (this feature)

```text
specs/008-model-fallback/
├── plan.md              # This file
├── spec.md
├── research.md
├── data-model.md
├── quickstart.md
├── contracts/
│   ├── config-schema.md
│   └── log-api.md
└── tasks.md             # Generated by /speckit.tasks
```

### Source Code (repository root)

```text
internal/
├── config/
│   ├── config.go           # ModelMapping.Fallbacks; FindModel returns []ModelMapping;
│   │                       # Load-time flatten + validate + cycle-detect
│   └── config_test.go      # Tests: validation, cycle detection, flattening, FindModel
├── proxy/
│   ├── handler.go          # ServeHTTP retry loop; isFallbackStatus; logRequest +attemptCount
│   ├── handler_test.go     # Fallback tests: 5xx retry, 429 retry, 4xx no-retry, exhaustion,
│   │                       # streaming pre-stream fallback
│   ├── openai_handler.go   # Same retry loop as handler.go
│   └── openai_handler_test.go
└── store/
    ├── store.go            # RequestLogEntry + RequestLogSummary gain FallbackAttempts int64
    ├── sqlite.go           # INSERT and SELECT SQL updated for fallback_attempts
    └── migrations/
        └── 014_add_fallback_attempts.sql

internal/web/
└── (log detail/list handlers — read FallbackAttempts from store; no template changes required
    unless UI displays the field, which is out of scope for this feature)
```

**Structure Decision**: Single project, modifications to three existing internal packages (`config`, `proxy`, `store`) plus one new migration file. No new packages.

## Phase 0: Research

See [research.md](research.md). All decisions resolved; no NEEDS CLARIFICATION items remain.

Key decisions:
- **Chain representation**: `ModelMapping.Fallbacks []string`; mutually exclusive with `Provider`/`UpstreamModel`
- **Resolution**: Eager flatten at `Config.Load`; `FindModel` returns `[]ModelMapping`
- **Fallback triggers**: `err != nil` from `SendRequest`, `status >= 500`, `status == 429`
- **Streaming boundary**: Check `provResp.StatusCode` after `SendRequest`, before `handleStreaming`; close stream on fallback
- **Logging**: `fallback_attempts INT NOT NULL DEFAULT 1`; `logRequest` gains `attemptCount int` param

## Phase 1: Design & Contracts

### Data Model

See [data-model.md](data-model.md).

**`internal/config.ModelMapping`** — add `Fallbacks []string`

**`internal/config.Config`** — add unexported `resolvedChains map[string][]ModelMapping` populated at `Load`

**`FindModel` signature change**:
```
Before: FindModel(name string) (*ModelMapping, error)
After:  FindModel(name string) ([]ModelMapping, error)
```

**`internal/store.RequestLogEntry`** — add `FallbackAttempts int64`

**Migration 014**: `ALTER TABLE request_logs ADD COLUMN fallback_attempts INTEGER NOT NULL DEFAULT 1`

### Contracts

See [contracts/config-schema.md](contracts/config-schema.md) and [contracts/log-api.md](contracts/log-api.md).

### `isFallbackStatus` helper

```go
func isFallbackStatus(code int) bool {
    return code >= 500 || code == 429
}
```

Lives in `internal/proxy/` (unexported, used by both handlers).

### Handler retry loop (both `Handler` and `OpenAIHandler`)

Pseudocode for the dispatch section of `ServeHTTP`:

```
chain, err = cfg.FindModel(reqBody.Model)   // []ModelMapping
if err → 400 unknown model

for i, mapping := range chain:
    prov = providers[mapping.Provider]
    provResp, err = prov.SendRequest(ctx, req_with_upstream_model)

    if err != nil || isFallbackStatus(provResp.StatusCode):
        if err == nil: provResp.Stream.Close()   // don't leak open connection
        attempts++
        if i == len(chain)-1: → 503 all entries exhausted (log attempts)
        continue

    // success
    if streaming:
        handleStreaming(... attempts+1)
    else:
        handleNonStreaming(... attempts+1)
    return

// not reached
```

`logRequest` gains a final `attemptCount int` parameter; callers pass `i+1`.

### Config load additions (in `Config.Load`)

```
1. For each ModelMapping in cfg.ModelList:
   - Validate mutually exclusive fields
2. Build name→mapping index
3. Cycle detection: DFS over virtual entries
4. Flatten: for each virtual model, recursively expand fallbacks into []ModelMapping
5. Populate cfg.resolvedChains
```

`FindModel` checks `resolvedChains` first (covers both direct and virtual), then falls back to wildcard logic for direct entries not in the map.

### Constitution Re-check (post-design)

All principles continue to pass. The retry loop adds one allocation (the `chain` slice, pre-computed) and a branch per attempt. No provider interface changes. Translation functions untouched.
